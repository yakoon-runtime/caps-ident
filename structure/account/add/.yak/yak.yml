title: Account Add
resolvable: true
navigable: false
contextual: false
host: /boot/python/runtime
entry:
  run: pack:y5n.caps.ident.apps.accounts.add:main
invocation:
  params:
  - key: username
    positional: true
    required: true
  - key: password
  - key: name
  - key: mail
  - key: language
resources:
  ref: resource:y5n.caps.ident.resources.loader:content
  document:
    default:
      path: account/add/default.ydf
  man:
    default:
      path: account/add/man.ydf
